overwrite: true

schema: 'https://api-sa-east-1.graphcms.com/v2/cl0k3mh7j4o9w01w75sjoag9s/master'

documents: './src/graphql/**/*.graphql'

generates:
  src/generated/graphql.tsx:
    plugins:
      - 'typescript'
      - 'typescript-operations'
      - 'typescript-urql'
    config:
      withHooks: true

hooks:
  afterAllFileWrite:
    - eslint --fix